const host = '';
const hostV1 = host + '/v1';
const hostV2 = host + '/v2';
const hostV3 = host + '/v3';
const hostV4 = host + '/v4';  

const urls = {
    xxx:hostV1+'/api/xxx',
}

export default urls;
